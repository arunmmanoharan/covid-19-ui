trigger:
  - master

pool:
  vmImage: "ubuntu-latest"
steps:
  - task: NodeTool@0
    inputs:
      versionSpec: "14.x"
    displayName: "Install Node.js"

  - script: yarn install
    displayName: "Install packages"

  - script: yarn build
    displayName: "Build app"
    env:
      CI: true